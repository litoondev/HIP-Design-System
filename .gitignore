# deps
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Standalone HTML style guide — the source the app was ported from.
# Kept on disk as the design reference, untracked so the repo ships only the app.
design-system/*.html
design-system/styles.css

# Raw images scraped from a client's old site — staging only, slot-named copies in
# public/sections/ are what ship
public/sections/_incoming/

# Next build output, wherever it appears (a stale copy was committed before .gitignore existed)
.next/
